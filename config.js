//Passwords and stuff

export default {
  nightmare: {
    show: true,
    width: 1200,
    height: 900,
    typeInterval: 500, // How long to wait between keystrokes when using .type()
    pollInterval: 500, // How long to wait between checks for the .wait() condition to be successful
    waitTimeout: 5000 // This will throw an exception if the .wait() didn't return true within the set timeframe
};

//Passwords
export{
  
}
